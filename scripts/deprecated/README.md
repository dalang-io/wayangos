# scripts/deprecated/

Historical build scripts kept for reference. They are **not maintained** and
**not used by CI or the current build pipeline**.

The active scripts live one level up in [`../`](../):

- `fetch-sources.sh`
- `build-kernel.sh`
- `build-rootfs.sh`
- `build-iso.sh`
- `build-pos-iso.sh`

Feel free to mine these for ideas, but do not rely on them. They will likely be
removed in a future cleanup.
